#include <iostream>
using namespace std;

int main() {
    cin.tie(NULL); cout.tie(NULL);
    ios_base::sync_with_stdio(false);

    int cnt, avg;
    cin >> cnt >> avg;
    cout << cnt * (avg - 1) + 1;
    return 0;
}
